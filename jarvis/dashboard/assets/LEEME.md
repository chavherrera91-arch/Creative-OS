# Assets de Jarvis

Coloque aquí su música de bienvenida como `welcome.mp3` (o cambie la ruta en
`jarvis/config/config.json` → `musicaBienvenida`).

Si no hay archivo, Jarvis reproduce una fanfarria sintetizada propia.
