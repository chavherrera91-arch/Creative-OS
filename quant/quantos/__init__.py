"""quantos — AI quant research platform.

A multi-agent Investment Committee that researches markets, validates every
idea through a backtest -> walk-forward -> Monte Carlo -> paper-trading funnel,
and **only paper-trades** (invariant I1: no code path places a live order).

The core imports with only numpy + pandas installed (invariant I6).
"""

__version__ = "0.1.0"
